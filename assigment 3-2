testscoreScoreString = input("Enter test Score: ")
classRankString = input ("Enter class Rank:")

testScore = int(testscoreScoreString)
classRank = int(classRankString)
#Decide to accept based on test score and class rank

if testScore >= 90:
    if classRank >= 25:
        print("Accept")
else:
    print("Reject")
else:
    if testScore >= 80:
        if classRank >= 50:
            print("Accept")
        else:
            print("Reject")

    else:
        if testScore >= 70:
            if classRank >= 75:
                print("Accept")
        else:
            print("Reject")
